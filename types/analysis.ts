/**
 * `/api/analyze` 구조화 JSON — 단일 출처는 `lib/ai/schema.ts`의 Zod 스키마입니다.
 */
export type {
  EvidenceStance,
  PresentationAnalysis,
  PresentationEvidence,
  PresentationIssue,
} from "@/lib/ai/schema"
